/**
 * Delete 注解
 * 
 * 定义在 bean 对象的方法上，被修饰方法会被覆盖
 * 
 * @author zhangbaili
 * @since 1.1.0-beta
*/

import HttpAnnotationGenerator from '../HttpAnnotationGenerator'

export default function Delete(path: string) {
    return HttpAnnotationGenerator.generate('DELETE', path)
}