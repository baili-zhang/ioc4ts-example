/**
 * PathVariable 注解
 * 
 * 定义路径变量
 * 
 * @author zhangbaili
 * @since 1.0.8-beta
*/

import ParameterGenerator from '../../core/ParameterGenerator'

export default function PathVariable(name: string) {
    return ParameterGenerator.generate(name, 'PathVariable')
}