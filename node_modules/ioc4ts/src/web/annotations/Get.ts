/**
 * Get 注解
 * 
 * 定义在 bean 对象的方法上，被修饰方法会被覆盖
 * 
 * @author zhangbaili
 * @since 2021/10/23
*/

import HttpAnnotationGenerator from '../HttpAnnotationGenerator'

export default function Get(path: string) {
    return HttpAnnotationGenerator.generate('GET', path)
}