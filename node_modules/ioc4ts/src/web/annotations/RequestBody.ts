/**
 * RequestBody 注解
 * 
 * 定义 http 请求体参数
 * 
 * @author zhangbaili
 * @since 1.1.0-beta
*/

import ParameterGenerator from '../../core/ParameterGenerator'

export default function RequestBody () {
    return ParameterGenerator.generate(undefined, 'RequestBody')
}