/**
 * RequestHeader 注解
 * 
 * 修饰方法的参数, 定义 http 请求头
 * 
 * @author zhangbaili
 * @since 2021/10/23
*/

import ParameterGenerator from '../../core/ParameterGenerator'

export default function RequestHeader(name: string) {
    return ParameterGenerator.generate(name, 'RequestHeader')
}