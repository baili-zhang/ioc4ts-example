/**
 * View 注解
 * 
 * @author zhangbaili
 * @since 1.1.3-beta
*/

import ApplicationContext from "../.."

export default function View (id: string) {
    return (ctor: any) => {
        const viewBeanFactory = ApplicationContext.getInstance().getViewBeanRegisty()
        viewBeanFactory.registerView(id, ctor)
    }
}