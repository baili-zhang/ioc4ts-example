/**
 * WebApplication 类
 * 
 * @author zhangbaili
 * @since 1.2.2-beta
*/

import ApplicationContext from "../core/ApplicationContext";

export default class WebApplication {
    run() {
        ApplicationContext.getInstance().getViewBeanRegisty().mountAll()
    }
}